name: Server

on:
  push:
    branches: [ main]
  pull_request:
    branches: [ main ]

jobs:
  deploy:
    uses: giocaizzi/python-dev-actions/.github/workflows/deployment.yml@toml
    with:
        subdir : repomanager
        python-version: "3.10"
      